# sound_terminal

## Build and Run

This project uses Maven. To build the project:

```bash
mvn package
```

Run the main class:

```bash
mvn exec:java -Dexec.mainClass=com.example.Main
```

Note: Internet access may be required to download dependencies and run the example request.

